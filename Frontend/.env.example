VITE_API_BASE=http://127.0.0.1:8000
VITE_USER_ID=demo-athlete

# Stripe secret keys never go in the frontend. Booking payment uses a backend
# Stripe Checkout endpoint and returns through /checkout/complete in a popup.
