# Dev 3 — Agent layer (Fetch.ai uAgents + Claude reasoning)
uagents>=0.22.0
anthropic>=0.40.0
httpx>=0.27.0
python-dotenv>=1.0.1
deepgram-sdk>=3.7.0
sounddevice>=0.5.0

# ASI:One bonus router (OpenAI-compatible client)
openai>=1.40.0

# Local mock backend, so the agent chain is runnable without Dev 2
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
